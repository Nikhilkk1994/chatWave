# Copy to `.env` in the project root. Never commit real keys.

# Session signing (use a long random string in production)
SESSION_SECRET=

# Anthropic — text companion (“Ask AI”)
ANTHROPIC_API_KEY=

# Optional
# CLAUDE_MODEL=claude-haiku-4-5
# PORT=3001
